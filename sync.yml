trigger:
  branches:
    include:
    - main

resources:
  repositories:
    - repository: templates
      type: git
      name: Plumbing/core-templates
    - repository: FixMyComputer
      type: git
      name: Open/FixMyComputer

stages:
- template: stages/sync/azure-sync.yml@templates  # Template reference
  parameters:
    checkout: FixMyComputer
